name: Ingest News

on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger ingestion
        run: curl -X POST https://nfl-fantasy-backend-2-h5j8.onrender.com/ingest
